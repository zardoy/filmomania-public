import React from "react";

import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
    events: {
        // position: "fixed",
        // top:
    }
});

interface ComponentProps {
}

let Overlay: React.FC<ComponentProps> = () => <></>;

export default Overlay;
